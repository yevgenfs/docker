spring:
  datasource:
    url: jdbc:mysql://lab3-mysql:3306/mydb?serverTimezone=Europe/Kiev
    username: root
    password: Fredikfs123456
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    # Show or not log for each sql query
    show-sql: true
    hibernate:
      # Hibernate ddl auto (validate, create, create-drop, update)
      ddl-auto: create
      # Naming strategy
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy

server:
  port: 10222

